// Dev-only primitives gallery. Reach it at jernie://dev/ui (or /dev/ui on web).
// The screen itself is src/ui/__gallery.tsx; this is only the route.
export { Gallery as default } from '@/src/ui/__gallery';
