export { DetailSheet, type DetailSheetRef } from './DetailSheet';
export { BLOCK_ORDER, TYPE_NAME, blocksFor } from './layout';
export { buildDetailModel, detailRole } from './model';
export { useDetailSheet, type OpenOptions } from './useDetailSheet';
export type { BlockKey, DetailModel, DetailPayload, DetailSubject } from './types';
