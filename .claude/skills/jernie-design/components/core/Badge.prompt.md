Use to state the booking status of a row, never to label its type — the icon does that.

```jsx
<Badge label="Booked" tone="accent" />
<Badge label="Open" tone="warning" />
```

`solid` is reserved for "Today" on a day header.
