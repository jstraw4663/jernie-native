Use for anything multi-select or instantly-applied: Explore filters, vibe pills, map day selectors.

```jsx
<Chip label="Bar Harbor" variant="dropdown" />
<Chip label="Seafood" icon={<FishIcon />} selected />
```

Never put a chip row behind an Apply button — that is a filter sheet, not a chip.
