Use in place of an empty list. Never render an illustration-and-caption empty state where a prompt row would do.

```jsx
<PromptRow title="Where are you staying?" sub="Bar Harbor · 3 nights unbooked" action="Add" urgent />
```

Order them by what generates gaps: stay, then transport, then everything else.
