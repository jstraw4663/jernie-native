Use on the Profile header, or on a trip summary. Requires real numbers — a new user sees zeros, so design that state alongside it.

```jsx
<StatStrip onPhoto stats={[{value:'14',label:'Trips'},{value:'9',label:'Countries'},{value:'63',label:'Nights away'}]} />
```
